{
  "name": "todoist-quick-capture",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-chrome": "^0.8.0",
    "sharp": "^0.33.0"
  }
}
